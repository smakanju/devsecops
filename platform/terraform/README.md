# Terraform Platform Modules

This directory contains reusable Terraform modules and environment compositions for AWS-only infrastructure.

- `modules/` contains EKS, MSK, Aurora, Redis, and IAM/IRSA modules.
- `environments/` contains per-environment stacks (dev/stage/prod).
