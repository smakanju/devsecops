[project]
name = "agent-platform"
version = "0.1.0"
description = "AI agent control plane"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.27",
  "pydantic>=2.6",
  "langgraph>=0.0.40",
  "boto3>=1.34",
]

[project.scripts]
agent-platform = "app.main:main"
